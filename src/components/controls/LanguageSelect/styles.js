import { makeStyles } from '@material-ui/core'

export default makeStyles(theme => ({
  container: {
    width: 70,
  },
  optionWrap: {
    flex: 1,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
}))
